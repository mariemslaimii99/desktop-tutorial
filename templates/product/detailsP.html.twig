{% extends 'base.html.twig' %}

{% block body %}
    <h1> {{  product.nomP}}</h1>
    <h2>{{ product.prix }}</h2>
    <h2>{{ product.image }}</h2>

    <h2>{{ product.description }}</h2>

    <p><a href="{{ path('products') }}">Retour</a></p>
{% endblock %}
